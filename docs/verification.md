# 本次验证记录

状态：2026-08-31 的验证证据快照，不是规范性设计文档。

## 1.0.13 Windows 验证状态（候选，尚未放行）

目标机对 1.0.12 的实测再次撤回了该制品：运行时目录的访问拒绝已经由同一个安装进程自动恢复，但随后当前用户对 `HKCU\Software\Policies\Google\Chrome\ExtensionInstallForcelist` 没有写权限。安装器把本来只是便利项的本地 CRX 策略写入放在全局致命错误域中，因而没有进入既有的人工“加载已解压的扩展程序”等待，直接以访问拒绝停止。这是可选自动化与安装前提分类错误；目标机不应承担发现该分支的职责。

1.0.13 候选把更新清单、策略键读/建/写、策略写后核对和自动启动浏览器统一限制在可选策略尝试内。未发生策略变更时，任一失败都记录 `OFFLINE EXTENSION POLICY UNAVAILABLE` 并直接显示扩展页地址与完整目录，在原安装进程等待人工加载；自动打开扩展页和 `clip.exe` 失败也只是警告。策略值已经实际写入时仍必须恢复原值，只有恢复失败才因状态不安全停止。为避免继续逐项暴露同类问题，运行时、扩展、配置备份/发布和配置回滚现共用 `[IO.Directory]::Move` 原子移动与有界重试；旧版留下的无效扩展目录会先保留到唯一备份路径再自动重建，失败配置也隔离而不递归删除。

本地已执行 90 项完整测试：88 项通过，2 项仅因需要 Windows PowerShell 5.1 而跳过；GitHub Actions YAML、全部 Python AST、shell 语法和 `git diff --check` 通过。新增 Windows package 验收会先预置并核对自动隔离的不完整扩展遗留目录，再对精确 Chrome 策略键真实添加当前用户 `SetValue` 拒绝 ACL，证明操作系统确实拒绝写值后，要求同一 launcher 依次记录策略不可用、人工等待、目录 ACL 自动恢复、安装成功和实际 MCP/Cookie 登录态复用 E2E，并逐字恢复原策略 ACL。该 Windows PowerShell 5.1 验收尚未运行完成前，本候选不得交付。

[Windows release validation #50](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33372817902) 的 `windows-2022`、`windows-latest` 两套源码/AST job 已通过；package job 在启动目标 launcher 前由新增故障注入夹具失败，未生成 artifact。GitHub Runner 原本没有 Chrome 策略父键，PowerShell Registry provider 的单次 `New-Item -Force` 不会递归创建完整父链，随后 `Get-Acl` 得到路径不存在。[#51](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33373444807) 改用 .NET `RegistryKey.CreateSubKey` 后两套源码 job 仍通过，但 package job 紧接着通过 PowerShell Registry provider 读取同一路径时仍不可见，同样在 launcher 前失败且没有 artifact；这证明夹具混用创建 API 与 ACL Provider 本身是不可靠验证。当前候选让夹具和安装器统一只通过 PowerShell Registry provider 从 `HKCU:\` 逐段创建并在每一段后立即用同一 Provider 核对，记录本次新建的全部段并在回退时逆序删除空键。夹具必须确认同一 Provider 可见后才施加真实拒绝；安装器任一段创建失败仍属于可选策略不可用并直接人工回退。两个失败 run 都没有冒充安装通过或生成可交付制品。

## 1.0.12 Windows 历史验证状态（已撤回）

目标机对 1.0.11 的实测撤回了该制品：Claude Code 本身可以正常使用，但顶层 launcher 在安装前运行了完整发布测试；其中 `test_self_test_runs_as_a_real_subprocess` 又以 30 秒硬超时启动注册器 `self-test`，目标 Python 子进程超过时限后以 `Python release check failed with exit code 1` 停止。该失败发生在开发测试，不是 MCP、Claude Code 或安装事务的功能失败。此前 GitHub Runner 在 30 秒内通过只能证明该 runner 足够快，不能证明把发布测试放到任意内网终端是可靠设计。

1.0.12 按 ADR 0008 拆分职责：`verify-windows-release.ps1` 保留完整单元测试、AST 解析、假 Claude 注册故障矩阵、真实隔离注册和 MCP 握手，但只由发布方 Windows CI 调用；目标 `INSTALL-WINDOWS-PILOT.cmd` 只启动一次安装器，安装器不再调用发布门禁或注册器 `self-test`。目标仍验证迁移包完整性、Windows/Python/现有 Claude、离线扩展、包内 Node、最终 MCP 握手和事务化真实 user-scope 注册，并在持久化变更前拒绝非 Windows x64 原生构建、`crossBuilt=true` 或 `targetCliSmokeTested=false` 的候选包。Windows CI 必须分别保留发布门禁与单次安装日志，并断言安装日志没有发布自检标记。

本地已执行 89 项完整测试：87 项通过，2 项仅因需要 Windows PowerShell 5.1 而跳过；新增用例还以真实校验器子进程实际接受原生正式元数据，并拒绝交叉构建、字符串伪装布尔值、未完成目标 CLI 冒烟、版本/archive 漂移和低于要求的 Node 元数据。Bash、全部 JSON、GitHub Actions YAML、全部 Python AST 与 `git diff --check` 也已通过。

[Windows release validation #48](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33367097614) 曾在提交 `e29f7d5b630f0974bc0b6051977408a5c49380fc` 上全绿，总耗时 4 分 49 秒；两套 Windows PowerShell 5.1 源码 job 与 Windows 原生 package/单次安装 job 全部成功。下载的发布者门禁日志在 Windows PowerShell 5.1 下运行 89 项测试，87 项通过、2 项按迁移包边界跳过，并继续通过注册器真实子进程 `self-test`、现有原生 Claude Code、三层 bundle、包内 Node 和 MCP stdio 门禁。目标安装日志只出现一次 `Windows pilot installer started`，先记录 `WINDOWS RELEASE METADATA: VALID`，在真实目录 ACL 访问拒绝后由同一安装进程记录 `RUNTIME PUBLISH RETRY 1/25` 与 `RUNTIME PUBLISH RECOVERED: attempts=2`，最后记录 `SUCCESS: installation and preflight completed`；该日志逐字检查不含 `self-test`、`test_self_test_runs_as_a_real_subprocess` 或 `unittest`。package job 还以实际安装的 Node + Playwright MCP 完成离线 Chrome 页面和 Cookie 登录态复用 E2E。该 run 没有锁定扩展策略注册表 ACL，目标机随后证明它漏掉了系统性分支，因此此前的 **WINDOWS CI-VERIFIED PILOT 1.0.12** 结论已撤回。

该 run 的 GitHub 迁移 artifact ZIP SHA-256 为 `1bd8e795d07d4973adcb5d461f6a83e9241ea13c523d0343363ba2bd5f2be066`，证据 artifact ZIP 为 `813e7e112a8491b842b04081766ad8abd2a3738f0de3bb04db3c180a6ccb3180`；下载后迁移 archive SHA-256 为 `5c836dc781a8c174577b3f3b14911f97078f10c52b53aaaee621f26119bd7b68`，包内 Windows 原生 runtime archive 为 `fc13c913e7e5e5101b1b8fc2cf6c4661a2526a05fedbc62db2fc9cc8760161e5`。迁移 archive、解压迁移目录、runtime archive、解压 runtime 目录四层均由当前验证器返回 `VALID`；两级 sidecar 均以单个 LF 结尾并实际通过 `shasum -a 256 -c`。元数据记录 69 个审查白名单文件，`buildHost=windows/x64`、`target=windows/x64`、`crossBuilt=false`、`targetCliSmokeTested=true`、`bundledNode=true`，包内 Node 为 v24.19.0、pnpm 为 11.19.0。

## 1.0.11 Windows 历史验证状态（已撤回）

1.0.11 修复目标机已经完成运行包和包内 Node 验证后，`staging\r-*` 到最终版本目录的发布因 Defender/EDR 短暂持有目录句柄而第一次访问被拒绝时立即退出的问题。Windows 实机 #44 进一步证明 PowerShell FileSystem provider 的 `Move-Item` 会在拒绝后留下源、目标同时存在的中间态，现改用 Windows PowerShell 5.1 中由 Win32 `MoveFile` 支撑的 `[IO.Directory]::Move` 保持同盘发布的原子失败语义。安装器只在源仍存在、目标尚未出现且错误属于访问拒绝、共享冲突或等价目录移动 I/O 错误时，在原安装进程内有界退避重试；释放后继续并再次验证最终目录，目标已出现或状态不明确时仍失败关闭。Windows package job 必须保存运行时目录和其唯一暂存父目录的原 ACL，在完整解压前对当前用户同时真实拒绝运行时目录的 `Delete` 与父目录的 `DeleteSubdirectoriesAndFiles`，以强制首次移动返回操作系统访问拒绝；观察到首次重试后逐字恢复两份原 ACL，并证明同一个 launcher/installer 记录 `RUNTIME PUBLISH RETRY` 与 `RUNTIME PUBLISH RECOVERED` 后完成安装、注册和登录态 E2E。

[Windows release validation #46](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33358623492) 已在提交 `f323b5bdf959e6b3283673749221b54cee1f5431` 上全绿，总耗时 5 分 37 秒。两套 Windows PowerShell 5.1 源码 job 各运行 84 项测试并通过；package job 使用两层真实 ACL 拒绝让首次 `[IO.Directory]::Move` 返回操作系统访问拒绝，随后在同一个 launcher/installer 进程中依次记录 `CI RUNTIME PUBLISH ACCESS DENIED ARMED`、`RUNTIME PUBLISH RETRY 1/25`、ACL 恢复、`RUNTIME PUBLISH RECOVERED: attempts=2` 和安装成功。安装日志中 `Windows pilot installer started` 仅出现一次，证明恢复没有要求用户重启安装。实际安装的 MCP 最终输出 `PLAYWRIGHT EXTENSION MCP E2E PASSED: server=1.63.0-alpha-2026-08-05, tools=30, session_cookie=reused`。结论为 **WINDOWS CI-VERIFIED PILOT 1.0.11**。

该 run 的 GitHub 迁移 artifact ZIP SHA-256 为 `195da4fc314d6ca13fc9da3fe56a7e263f91a5e005657e407251fd6a0baed77e`，证据 artifact ZIP 为 `9a41c4dcef0b6a0b53495b656227cf43ffe9ce39de6a88e2319ca3287769d02f`；下载后迁移 archive SHA-256 为 `7d1a9bbbabd0c5def3113dd6541b58a13076186759b03728fd38e695c034830b`，包内 Windows 原生 runtime archive 为 `682da63be0bf31034386eacbf3d18dcd79b82573a17d3ca0eafcabc511ceb02c`。迁移 archive/目录及 runtime archive/目录四层均由当前验证器返回 `VALID`。两个 Windows 生成的 sidecar 都逐字节确认为 UTF-8/ASCII 且只以单个 LF 结尾，标准 `shasum -a 256 -c` 均实际通过，修复了 #45 的 CRLF 可移植性缺陷。元数据为 `buildHost=windows/x64`、`target=windows/x64`、`crossBuilt=false`、`targetCliSmokeTested=true`，包内 Node 为 v24.19.0、pnpm 为 11.19.0。不得用本地交叉构建、#45 制品或旧 1.0.10 制品冒充本次放行。

## 1.0.10 Windows 验证状态

目标机实测撤回了 1.0.9：目标机通过 npm 安装的 Claude Code 已经可以正常使用，入口是 `claude.cmd`，而发布门禁和安装器把“可用 Claude Code”错误限定为原生 `claude.exe`，因此在任何安装变更前假阴性停止。这是需求边界、规范、实现和 CI 同时固化了错误假设的系统性缺陷，不是目标机缺少 Claude Code，也不授权安装器替用户安装或替换 Claude Code。

中间候选提交 `b47c40f` 只把原生 Claude 路径解析收敛到共享 helper，并在 [Windows release validation #33](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33348472314) 通过了“`PATH` 不可见、官方每用户位置存在”的原生分支；该 run 没有覆盖 npm 入口，而且上传 artifact 的显示版本仍误写为 1.0.9，所以即使 run 全绿也不得交付。最终提交 `7f6c4c8` 在同一个共享探测器中支持两类现有入口：原生 `claude.exe` 直接执行；标准 npm `claude.cmd` 解析同级 `@anthropic-ai/claude-code` 包的 bin 与现有 `node.exe` 后，直接执行该安装的 `node.exe + cli.js`。目标机不运行 npm/pnpm/npx，也不安装、升级、替换或修复 Claude Code。

[Windows release validation #37](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33350818118) 已完整通过原生 stale-PATH、npm PATH 优先、真实 npm Claude 隔离注册、真实原生 Claude 隔离门禁，以及 npm 入口下的一次 launcher、离线扩展续跑和 MCP/Cookie E2E。结论更新为 **WINDOWS CI-VERIFIED PILOT 1.0.10**。GitHub 迁移 artifact ZIP SHA-256 为 `f0dd1fe2e3f8610e263e358ed0146703c0b0daca3eb8bdbb62293fd632a6832f`，证据 artifact ZIP 为 `42b99583053db7e9b62c80fab00a8c652d18a8f6501535873278e453047b8a83`；下载后迁移 archive SHA-256 为 `a2c6d21c69eed6eb627536839d71d49b0305967dc272cbb4bbc84f27d6cdded4`，包内 Windows 原生 runtime archive 为 `491985a2cb341aed54d129d516eeb1da0d5a7b19b7f922014cf24b02a66c6bc4`。迁移 archive/目录及 runtime archive/目录四层均由当前验证器返回 `VALID`。

新的 npm 全链路 CI 夹具固定使用目标机日志中的 Node v20.18.3，而不是用构建 Node v24.19.0 掩盖兼容差异；包内 Playwright MCP 仍使用批准的 v24.19.0。

## 1.0.9 Windows 历史验证状态（已撤回）

1.0.9 改为 Windows pilot `extension` 模式，迁移包携带固定官方 CRX 和逐文件一致的已解压副本；安装器先尝试完全离线策略安装，失败时恢复临时策略、打开扩展页并在原进程默认无限等待用户加载，检测成功后继续。真实调用复核证明 pipe-only `Extensions.loadUnpacked` 当次可返回固定 ID、版本和 `enabled=true`，但 Chrome 再次启动会清除该状态，所以它不能冒充人工“加载已解压扩展”的持久安装。GitHub 托管 runner 又逐项证明桌面模拟、CDP 拖放、目录 input 和临时 FileSystem 都无法产生 Chrome 原生目录选择器授予的受支持 `DirectoryEntry`。1.0.9 因此明确拆分人工边界：源码测试覆盖三步提示、默认无限等待和同进程续跑；Windows 原生 job 只在一个持续存活的隔离 Chrome 会话中加载精确批准目录，用它触发原安装器续跑，再由实际安装的 MCP 完成离线页面 `navigate + snapshot + Cookie` E2E。该 CI 替身不声称重启持久性；目标机仍按屏幕提示完成人工三步且无需重启安装器。Windows release validation #31 已完整通过该链路并生成放行制品。

固定 `@playwright/mcp` 0.0.79 的另一项真实差异也已修复：其默认扩展预检不能识别只写入 Chrome `Secure Preferences` 的手工 unpacked 扩展。Windows 清单、渲染、握手和 Claude user-scope 注册现在共同固定自动识别的 browser `.exe`，生成精确 `--browser` 与 `--executable-path`；安装检测只允许 `Local State.profile.last_used` 指向的 Profile，避免休眠 Profile 误放行。该路径仍不保存扩展 token，仍由用户在实际使用时批准目标 Tab。

历史 CI 结论曾为 **WINDOWS CI-VERIFIED PILOT 1.0.9**：GitHub 托管的 Windows x64 环境完成了 80 项源码测试、Windows 原生重建、PowerShell 5.1 发布门禁、真实 Claude Code 隔离注册、一次顶层 launcher 安装、人工回退等待后的同进程续跑，以及实际安装 MCP 对离线页面的 `navigate + snapshot + Cookie` 登录态复用 E2E。由于该 runner 显式刷新了 Claude 安装目录到 `PATH`，它没有覆盖目标机已经证明会失败的 stale-PATH 分支；因此 1.0.9 已撤回，不能继续作为交付包。

1.0.6 已撤回：其真实 Windows PowerShell 5.1 门禁在字节保真测试中暴露 CRLF 夹具缺陷。1.0.7 也在交付前撤回：复核发现把 `.cmd` 作为实际 MCP 命令、Node 来源只做自洽校验，以及运行时/配置发布事务不够严格。1.0.8 才包含直接 `node.exe + cli.js`、固定 Node 批准哈希、固定 MCP 子进程环境、真实 MCP stdio 握手和本记录所述的事务收紧。

## 已执行并通过

- 源码测试：当前本地构建环境执行 `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v`，90 项中 88 项通过、2 项仅因需要 Windows PowerShell 5.1 而跳过。新增覆盖明确断言策略访问、浏览器自动启动和剪贴板失败进入同一进程人工回退，扩展与配置所有目录切换共用原子重试且安装器不含 `Move-Item`；其余覆盖包括目标 launcher/installer 不调用发布门禁、`unittest` 或注册器 `self-test`，顶层 launcher 只产生一个安装器进程，以及 Windows 路径、user scope、版本一致性、LF/CRLF 逐字节备份、规范 LF archive sidecar、`remove/add/get` 注册失败回滚、条目/环境错写、窄 Windows 代码页与 Claude UTF-8 输出组合、遗留环境清理、Node 来源、archive 路径、扩展 CRX/Profile/人工等待、休眠 Profile 防误判、显式 browser `.exe`、配置漂移、发布顺序、发布访问拒绝恢复和 MCP stdio 握手。run #48 的 89 项 Windows 结果仅属于已撤回的 1.0.12 历史证据；1.0.13 的 Windows 结果待本节候选门禁完成后补充。
- 语法与格式：全部 Python 文件通过编译检查；shell 入口通过 `bash -n`；12 个 JSON/JSON template 通过标准 JSON 解析；`git diff --check` 通过。
- PowerShell 解析：`build-offline-bundle.ps1`、`INSTALL-WINDOWS-PILOT.ps1`、`verify-windows-release.ps1` 均由官方 PowerShell 7.6.5 parser 返回无 AST 错误；GitHub Actions 的 `windows-2022`、`windows-latest` 两个 job 又分别使用 Windows PowerShell 5.1 Desktop 解析仓库内全部 PowerShell 脚本并通过。
- 清单与渲染：本地 demo 返回 `VALID`。Windows pilot 模板固定 `mcpScope=user`、`workspaceRoots=[]`，省略 `network`/`dataBoundary`，只因明确占位符而失败关闭；production 模板继续因未批准字段和占位符失败关闭。Windows `.mcp.json` 的命令直接指向包内 `node.exe`，首个参数指向固定 Playwright CLI，不使用 `.cmd` shell shim；环境与 `config/windows-mcp-environment.json` 完全一致且不含扩展 token。
- 注册事务：`register_claude_user_mcp.py self-test` 作为真实子进程通过。自动用例证明 user-scope `remove → add → 实际用户配置核对 → get` 顺序、首次安装明确缺少旧条目继续、其他 `remove` 错误停止、成功 stderr 非致命、已有配置 LF/CRLF 逐字节备份、`remove/add/get` 失败恢复、条目或环境错写时恢复，以及原配置不存在时删除新配置。另以 `PYTHONIOENCODING=cp1252:strict` 启动真实注册入口，并让假 Claude 直接输出 UTF-8 中文 stderr；事务仍成功，证明注册状态文本不会再因窄代码页产生 `UnicodeEncodeError`。
- MCP 协议：`smoke_playwright_mcp.py` 直接启动 Node + 固定 CLI + 生成的 Windows pilot Playwright 配置，使用与最终注册相同的固定环境并发送 MCP `initialize`、`notifications/initialized` 和 `tools/list`。恶意测试值 `PLAYWRIGHT_MCP_CONFIG`/`NODE_OPTIONS` 会被清空。本机构建返回 Playwright server `1.63.0-alpha-2026-08-05`、30 个唯一工具；Windows 原生包内的 `node.exe` 在发布门禁中返回同一 server 版本、24 个唯一工具。两端均包含并核对 `browser_navigate`、`browser_snapshot` 等核心工具，Windows 结果是目标 EXE 的真实 stdio 执行证据。
- 固定依赖构建：使用 Node.js v24.19.0、pnpm 11.19.0、Python 3.12.13，依据冻结锁文件安装生产依赖，禁用安装脚本和 Playwright 浏览器下载。core 运行时只保留 `@playwright/mcp`、`playwright`、`playwright-core`，不含 Chrome DevTools MCP、pnpm 元数据或平台原生依赖。
- Node 来源：从 Node.js v24.19.0 官方 Windows x64 ZIP 与同版本 `SHASUMS256.txt` 生成四文件最小 distribution。实测 ZIP SHA-256 为 `57f71ab3652e797d84acddc79c81cc9ff1c6ddb2a1974cdb83f00fee9bff4c73`，`SHASUMS256.txt` 为 `be0629ee2bcd8e40bb856abdd3407f0762101b76bd60a36b8867f637733631c0`，`node.exe` 为 `3602f2bb1a10f2cbab4c36886218a33c1ab3db87290e73b033c46c77147d0237`，`LICENSE` 为 `d9c4eeda951d6d08f4aa1316b61aafcf67e6da5f79b18f8edeb56fa6abdc038c`；均与 `config/windows-node-sources.json` 一致。自洽但未批准的伪造来源回归会失败。
- 运行包：run #48 已由 GitHub Actions 在 Windows x64 上使用固定 Node v24.19.0、pnpm 11.19.0 原生重建 `browser-agent-runtime-1.0.12-core-windows-x64.tar.gz`；制品记录 `buildHost=windows/x64`、`target=windows/x64`、`crossBuilt=false`、`targetCliSmokeTested=true`、`bundledNode=true`，并以包内 `node.exe` 完成版本与 MCP 握手后才组装迁移包。任何本地交叉构建结果仍必须记录 `crossBuilt=true`、`targetCliSmokeTested=false`，不得冒充原生制品。
- 运行包结构：239 个 archive member，0 symlink、0 hardlink、0 device/FIFO，最长 archive 路径 146 字符；`SYMLINKS.json` 为空，`node_modules` 中无 `.exe/.dll/.node/.so/.dylib`，唯一目标 EXE 是批准的 `node/node.exe`，Node 目录精确包含 `LICENSE`、`SOURCE.json`、`VERSION`、`node.exe`。安装器使用 `%LOCALAPPDATA%\IntranetBrowserAgent\staging\r-*` 短暂存路径，降低传统 Windows 路径长度风险。
- archive 防护：校验器拒绝绝对路径、`..`、反斜杠、ADS 冒号、Windows 保留名、尾随空格/点、大小写碰撞、hardlink/device/FIFO、越界 symlink，以及用 symlink 冒充 `SHA256SUMS`/`SYMLINKS.json`。运行包和迁移包均无链接。
- 安装事务：运行时先在同盘短目录解压，完整性、Windows 原生发布元数据、Node 批准哈希和实际版本通过后才发布固定版本目录。发布遇到短暂访问拒绝时，仅在源仍存在且目标不存在的安全状态下由同一个安装进程有界重试，成功后重新验证最终目录；run #48 已用真实 ACL 拒绝证明首次失败、第二次恢复且安装器只启动一次。配置先在 `%LOCALAPPDATA%` 暂存并 preflight，再整目录切换；“旧目录备份完成”和“新目录发布完成”独立记账，备份动作本身失败时不会删除原目录。后续失败恢复旧配置和 Claude 用户配置。
- Windows 门禁：发布方在 Windows CI 中单独运行 `verify-windows-release.ps1`，完成单元测试、注册器 `self-test`、原生 Claude 隔离注册和 MCP 握手后才允许组装可下载制品。目标顶层 `.cmd` 规范化自身解压目录后只启动一次 `INSTALL-WINDOWS-PILOT.ps1`；不运行发布门禁、`unittest`、注册器 `self-test`、npm、pnpm 或 npx。run #48 的 package job 先用原生 Claude Code 2.1.84 完成独立发布门禁，再从 `PATH` 隐藏原生入口、暴露真实 npm `claude.cmd`，证明单次目标安装选择现有 npm 安装并以目标机同版本 Node v20.18.3/CLI 完成注册；CI 的 Internet 出站阻断只用于证明离线能力，不会由目标安装器修改用户防火墙。安装器从暂存目录访问拒绝自动恢复、从人工扩展等待自动续跑，并由实际安装 MCP 完成离线页面与 Cookie 登录态复用 E2E。
- 迁移包：1.0.12 共包含 69 个逐文件白名单内的源码/文档文件；仍不收录 `.git`、真实部署清单、`build/`、`dist/`、缓存或 Python bytecode。run #48 的迁移 archive、解压迁移目录、包内 runtime archive、解压 runtime 目录四层均返回 `VALID`；发布测试与注册器 `self-test` 的实现仍随包保留以便审计，但目标 launcher/installer 不调用它们。迁移 archive SHA-256 `5c836dc781a8c174577b3f3b14911f97078f10c52b53aaaee621f26119bd7b68` 与 LF sidecar 和验证证据三方一致，标准 `shasum -a 256 -c` 已实际通过。

## Windows Actions 实测事实

- 1.0.9 的 [Windows release validation #6](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33304962390) 在扩展探针启动 Chrome 时返回 Chromium `PROFILE_IN_USE`（exit code 21），未上传交付 artifact；候选随后只关闭托管 runner 的临时 Chrome 再接管隔离 Profile。
- 修复锁竞争后的 [Windows release validation #7](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33305261099) 中，两套 Windows PowerShell 5.1 源码 job 均通过，但原生打包 job 在“一次点击 package launcher”阶段失败且未上传 artifact。结合上述重启复现，原 CDP 当前会话加载门禁不足，现已从验收标准移除。新的 Chrome UI + 重启 MCP/Cookie 门禁尚待下一次 run 验证。
- [Windows release validation #8](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33307030856) 暴露了源码专用 CI 测试错误地假定迁移包内仍有 `.github` 目录，门禁因此在安装开始前停止且未上传交付 artifact；测试现会在迁移包环境明确跳过 CI 专用文件检查，workflow 也会先保留 launcher 原始退出码和诊断输出，不再用后置令牌检查掩盖原始失败。
- [Windows release validation #9](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33307301003) 的两套 Windows PowerShell 5.1 源码 job 通过，原生 job 也实际进入安装器的人工加载提示并保持原 launcher 等待，但 CI 替代用户操作的旧 `SendKeys` 探针只激活浏览器进程、没有锁定其模态目录选择框，最终未加载扩展并按门禁失败；没有上传交付 artifact。候选已改用独立 Windows PowerShell 5.1 UI Automation 脚本等待浏览器拥有的 `#32770` 目录对话框、写入其地址栏并调用明确的接受按钮；该脚本自身纳入两套 Windows AST 门禁，仍须由后续完整 run 证明。
- [Windows release validation #10](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33307793875) 证明上述独立 UI Automation 脚本已在两套 Windows PowerShell 5.1 job 通过 AST 解析，原生 launcher 再次进入人工加载等待；其桌面诊断只发现可见的 `Extensions - Google Chrome` 窗口而没有 Chrome 文件夹对话框，说明 DOM `HTMLElement.click()` 不足以形成 Chrome 接受的真实用户输入。该 run 失败且未上传交付 artifact。候选探针现改为在可见扩展页上定位按钮坐标并发送 DevTools `mouseMoved → mousePressed → mouseReleased` 输入序列，目录框出现后才交给 UI Automation；仍须由后续完整 run 证明。
- [Windows release validation #11](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33308074685) 的两套源码 job 通过，原生 launcher 也进入人工等待，但 Chrome 同样没有为 DevTools `Input.dispatchMouseEvent` 序列打开原生目录框；桌面仍只有扩展页，没有误把其他系统对话框当作成功，run 失败且未上传交付 artifact。候选现由 PowerShell UI Automation 在可见 Chrome 可访问性树中定位 `Load unpacked` 按钮，以其屏幕边界发送真实 Win32 指针移动、左键按下和释放，然后才寻找并控制浏览器拥有的目录框；仍须由后续完整 run 证明。
- [Windows release validation #12](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33308355373) 在两套源码 job 的 Windows PowerShell 5.1 AST 阶段一致停止，原生 job 未启动：新建的 BOM-less CI PowerShell 脚本包含中文按钮别名，5.1 按旧系统代码页读取后产生乱码语法 token。该 run 无 artifact。CI 专用选择器现只匹配英文托管 runner 的 `Load unpacked`，并保持全文件 ASCII；目标机安装器自身的中文人工指引未改变。
- [Windows release validation #13](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33308454810) 恢复两套源码 job 全绿并进入原生人工回退；Chrome 的真实 UI Automation 按钮集合包含 `Developer mode` 但没有 `Load unpacked`，证明预写 Profile 偏好没有让当前可见页面完成用户手册第 1 步。探针拒绝猜测坐标、失败且无交付 artifact。候选桌面脚本现先读取并以 Win32 鼠标点击 `Developer mode`，等待 `Load unpacked` 真正在可访问性树出现后再点击并控制目录框；仍须由后续完整 run 证明。
- [Windows release validation #14](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33308739127) 的源码 job 通过，原生回退已找到 `Developer mode` 控件，但 Chrome 顶层 UI Automation 元素拒绝 `SetFocus()`，探针在鼠标点击前失败关闭且无交付 artifact。候选保留 `ShowWindowAsync`、`BringWindowToTop`、`SetForegroundWindow` 和屏幕坐标点击，并把不受 Chrome 顶层元素支持的 UIA `SetFocus()` 降为可观测的 best-effort；仍须由后续完整 run 证明。
- [Windows release validation #15](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33308963355) 的源码 job 通过；原生回退读取到 `Developer mode` 的 UI Automation ToggleState 为 `Off`，把 Win32 指针移动到控件中心 `(981,180)` 并点击，但 Chrome 状态未改变，因而 `Load unpacked` 仍未出现。探针失败关闭且无交付 artifact。候选现优先调用控件公开的 UI Automation `TogglePattern.Toggle()` 并读取变化后状态；`Load unpacked` 出现后优先使用其 `InvokePattern.Invoke()` 打开目录框，屏幕坐标仅作不支持相应模式时的后备；仍须由后续完整 run 证明。
- [Windows release validation #16](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33309243701) 的源码 job 通过，原生 launcher 在 UIA 控件调用后持续停留、显著超过此前失败时长；这与 `InvokePattern.Invoke()` 等待其打开的模态目录框关闭、同一脚本因此无法处理目录框的行为一致。该挂起 run 已主动取消，无交付 artifact。候选现由独立 PowerShell 子进程发起 `Invoke()`，主选择器并发等待和填写目录框；目录框关闭后必须核对子进程按时以零退出，避免把异步启动误当成功。
- [Windows release validation #17](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33309572073) 再次让两套源码 job 通过，并首次以 `TogglePattern` 把可见 Chrome 的 `Developer mode` 从 `Off` 切到 `On`、找到 `Load unpacked`。但异步 PowerShell 调用器未进入其首条 `UIA_ASYNC_LOAD` 日志便非零退出；主脚本仅得到空 stderr，且 launcher 子进程没有被 workflow 完整收束，run 在确认挂起后主动取消，无交付 artifact。候选已删除该跨进程模态调用：主选择器直接对已出现的 `Load unpacked` 可访问性控件发送真实 Win32 鼠标按下/释放，再在同一有界进程中等待和填写 Chrome 原生目录框；仍须由后续完整 run 证明。
- [Windows release validation #18](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33310095109) 的两套源码 job 通过，原生回退再次把 `Developer mode` 从 `Off` 切到 `On`，并把 Win32 指针移动到实际 `Load unpacked` 控件中心 `(104,237)` 完成按下/释放；Chrome 仍未打开目录框，证明托管桌面的坐标输入不等同于该控件获得的键盘激活。探针已输出明确错误，但 Chrome 调试 pipe 仍让设置 `process.exitCode` 的 Node 事件循环存活，run 在确认失败后取消且无交付 artifact。候选现先以 UI Automation 把焦点设置到同一个已确认的 `Load unpacked` 控件，复核焦点进程和名称后发送桌面 Enter；失败入口改为立即 `process.exit(1)`，不再让诊断后的辅助进程拖住 launcher。仍须由后续完整 run 证明。
- [Windows release validation #19](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33310657390) 的两套源码 job 通过；原生回退成功把焦点精确设置到 Chrome 进程内名为 `Load unpacked` 的 UI Automation 控件并发送 Enter，但 Chrome 仍未打开目录框，说明托管 runner 不把该模拟键盘输入当作所需激活。失败入口已按预期立即退出，整个 launcher 步骤 1 分 24 秒结束，没有再挂起，也无交付 artifact。候选现把已确认的同一个 UIA 控件对象交给当前 PowerShell 进程内的专用 STA runspace 执行 `InvokePattern.Invoke()`；主 runspace 并发控制模态目录框，关闭后等待并核对调用返回，避免跨进程重新查找控件和鼠标/键盘信任差异。仍须由后续完整 run 证明。
- [Windows release validation #20](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33310997732) 的两套源码 job 通过；原生回退把同一个 `Load unpacked` UIA 控件交给 STA runspace 调用 `InvokePattern.Invoke()`，主 runspace 保持并发，但 Chrome 仍未生成目录框，说明问题不是 PowerShell 模态调用阻塞。该 run 同样及时失败且无 artifact。复核发现旧 Win32 路径调用 `SetForegroundWindow` 后没有验证实际前台 HWND；候选现附加当前、原前台和 Chrome 的输入线程，恢复并置顶 Chrome 后强制核对 `GetForegroundWindow()`，再用 `WindowFromPoint`/`GetAncestor` 证明按钮中心实际命中该 Chrome 顶层 HWND，全部成立才发送鼠标输入。仍须由后续完整 run 证明。
- [Windows release validation #21](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33311383910) 的两套源码 job 通过；原生回退也证明 Chrome 已成为真实前台窗口，按钮中心的 `WindowFromPoint` 顶层 HWND 与该 Chrome 完全一致，但桌面鼠标输入仍未让浏览器打开目录框。该 run 及时失败且无 artifact。这组证据把根因收敛到 GitHub 托管 runner 缺少可代表真人的受信任桌面输入，而不是坐标、焦点或窗口归属。候选现删除该不可证实的桌面模拟，改由 CI 通过 Chrome 扩展管理页自身的目录拖放处理器加载同一目录；仍禁止 `Extensions.loadUnpacked`，并继续要求关闭/重启后持久检测和真实 MCP/Cookie E2E。仍须由后续完整 run 证明。
- [Windows release validation #22](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33312019955) 的两套源码 job 通过，原生打包、门禁和安装器也进入同一人工等待点；CI 已通过 Chrome 内部 API 启用隔离 Profile 的开发者模式，但仅把目录作为 `Input.dispatchDragEvent` 数据投递到页面中心没有触发扩展管理页的 Shadow DOM 拖放处理器，`Extensions.getExtensions` 一直为空。该 run 失败且无交付 artifact。Chrome 源码确认页面实际分两步调用 `developerPrivate.notifyDragInstallInProgress()` 保存当前 WebContents 拖放数据，再以 `developerPrivate.loadUnpacked({useDraggedPath:true})` 持久加载；候选现于真实 `dragEnter` 之后显式调用同一对页面 API，并输出结构化返回值，仍须由后续完整 run 证明。
- [Windows release validation #23](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33313023139) 的两套源码 job 继续通过，原生 job 再次完成打包、门禁并进入同一人工等待点；Chrome 明确返回 `No dragged path`。这证明 DevTools `Input.dispatchDragEvent` 虽能合成页面拖放事件，却不会填充 Chrome `WebContents::GetDropData()` 的原生文件路径，因而显式调用页面同款 `notifyDragInstallInProgress()` 也不能把它伪装成真人目录拖放。该 run 失败且无交付 artifact。候选现删除拖放模拟：通过目录输入和 `DOM.setFileInputFiles` 取得真实 `DirectoryEntry`，再调用 Chrome 会写入 Profile 的 `developerPrivate.loadDirectory`；仍保持扩展 ID/版本/原路径、浏览器重启、安装器同进程续跑和 MCP/Cookie E2E 四重门禁，须由后续完整 run 证明。
- [Windows release validation #24](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33313547085) 的两套源码 job 通过，原生 job 完成打包、门禁并进入人工等待；`DOM.setFileInputFiles` 命令本身成功，但探针紧接着同步读取时得到 `fileCount=0`、`entryCount=0`，因此严格停止且没有交付 artifact。Chromium 自身的目录上传协议测试先注册 `input` 事件再读取文件，说明目录枚举是异步完成；候选探针现同样在设置路径前注册一次性 `input` 事件，并有 15 秒失败关闭上限，只有事件完成且取得唯一 `DirectoryEntry` 才允许调用持久加载实现。
- [Windows release validation #25](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33313788932) 的两套源码 job 通过，原生 job 也在人工等待后收到目录输入的 `input` 事件，枚举出 15 个文件及正确的 `unpacked/connect.html` 相对路径；但 Chrome 对 CDP 设置的 file input 明确保持 `webkitEntries=[]`，因此无法把它传给只接受 `DirectoryEntry` 的持久 loader。该 run 严格失败且无 artifact。候选现复用已经证明能到达 renderer 的 `Input.dispatchDragEvent`，但不再调用要求原生 `WebContents::GetDropData()` 的 `notifyDragInstallInProgress`；它直接从 DOM `DataTransferItem.webkitGetAsEntry()` 取目录对象并交给持久 loader，同时保留原路径和重启门禁。
- [Windows release validation #26](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33314138782) 的两套源码 job 通过，原生 job 完成打包、门禁并进入人工等待；renderer 收到了 1 个 `kind=file` 的合成拖放项，但 `webkitGetAsEntry()` 返回 `null`，证明 CDP 同样不会为合成拖放授予本地文件系统 Entry。该 run 严格失败且无 artifact。Chrome 源码还提供另一条持久分支：非本地 `DirectoryEntry` 会由 `developerPrivate.loadDirectory` 逐文件复制到 Profile 的 `Unpacked Extensions` 后交给 `UnpackedInstaller`。候选现使用已经实测可枚举的 15 个目录输入文件，在 Chrome 临时 FileSystem 中原样重建目录，再走该持久 importer；探针和安装检测器均要求导入副本路径固定且与批准目录逐文件类型、大小、SHA-256 一致。
- [Windows release validation #27](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33314585940) 的两套源码 job 通过 81 项测试；原生 job 重建迁移包、通过发布门禁并进入人工等待，随后在 Chrome 临时 FileSystem 中逐字节重建了全部 15 个扩展文件，但 `developerPrivate.loadDirectory` 明确返回 `DirectoryEntry of unsupported filesystem`。该 run 严格失败且无 artifact。这一结果封闭了托管 runner 内合成持久目录选择的最后一条已知 Chrome 路径；候选不再增加安装检测例外，也不再声称 CI 能替代真人原生目录选择，改用文首所述的显式会话替身分别验证安装器续跑和实际 MCP/Cookie 功能。
- [Windows release validation #28](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33315585302) 的两套源码 job 通过 80 项测试，原生 job 也重建迁移包、通过发布门禁、进入三步人工提示，并取得 `CI_SESSION_ONLY_EXTENSION_READY` 的精确 ID/版本/路径证据。等待中的原 launcher 随即从第 3 步自动继续，完成第 4 至第 6 步、实际 Claude user-scope 注册、最终 MCP stdio 握手和 preflight。外层 workflow 随后把 Windows PowerShell 5.1 `Start-Process` 对已退出 `.cmd` 返回的空 `ExitCode` 当作非零而误报失败，所以该 run 无交付 artifact，且尚未执行页面/Cookie E2E。候选包装器现把 launcher 和 Chrome 会话退出码分别写入独立 marker，workflow 严格解析后再判定，避免依赖该空属性。
- [Windows release validation #29](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33316036094) 验证了上述显式退出码 marker：两套源码 job 通过 80 项测试，原生 job 的同一个 launcher 再次从人工等待自动续跑，并完成发布门禁、安装、Claude user-scope 注册、30 工具 MCP stdio smoke 和最终 preflight。随后真实页面/Cookie E2E 的 MCP relay 已在 loopback 启动，但扩展没有接受连接。结合 relay 日志、候选探针在扩展初始化后覆盖 `localStorage` 的实现，以及 Playwright 官方测试从状态页读取扩展令牌的做法，首先修正为读取 `status.html` 的 `.auth-token-code`，只把扩展自身生成的令牌注入一次性测试子进程，不再覆盖扩展状态。该 run 严格失败且无交付 artifact。
- [Windows release validation #30](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33316581837) 证明扩展状态页令牌已成功读取、同一 launcher 也再次完整续跑，但 MCP relay 仍停在 `Waiting for incoming extension connection`，所以令牌不是唯一阻断点。Playwright 官方扩展 fixture 明确在启动持久 Chrome 时移除 `--enable-automation`，因为该参数会关闭扩展模式依赖的浏览器单实例行为；候选 CI 会话此前恰好携带该参数，导致 MCP 后续启动的 `chrome.exe .../connect.html` 不能转发给已加载扩展的运行中进程。该 run 严格失败且无 artifact。候选现移除该参数并以源码回归禁止恢复；仍须由下一次完整 run 证明。
- [Windows release validation #31](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33316966849) 全绿：`windows-2022`、`windows-latest` 两套源码 job 和 Windows 原生 job 全部成功。原生 job 从同一个顶层 launcher 进入三步人工回退等待，CI 会话加载精确批准扩展后原安装器自动续跑，完成发布门禁、安装、真实 Claude user-scope 注册和 preflight；随后实际安装的 Node + `@playwright/mcp` 建立扩展连接，对本机离线页面执行 `browser_navigate` 与 `browser_snapshot`，并输出 `PLAYWRIGHT EXTENSION MCP E2E PASSED: server=1.63.0-alpha-2026-08-05, tools=30, session_cookie=reused`。该 run 上传的迁移 ZIP artifact 为 37.1 MB、GitHub SHA-256 `81480ae7a82ed2f8c1a8f821de9dab7b277d389b8881274b570a99a1b3514262`，证据 artifact SHA-256 为 `b2984441b6181f5efd13dd83737d2a54ff24a7a0176d7491adab04355d96bce3`。下载后再次验证迁移 archive SHA-256 `501895a5822be16fa0d7e60b2ffd142a9f52d20a6e8184caa2d86e5a1a8f0c96` 与 sidecar 一致，迁移 archive/目录及 runtime archive/目录四层均为 `VALID`；元数据为 `windows/x64`、`crossBuilt=false`、`targetCliSmokeTested=true`。
- [Windows release validation #34](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33349992697) 首次加入 npm Claude 回归后在两套源码 job 暴露测试自身把 PowerShell 单元素 `Prefix` 当成标量字符串再取 `[0]`，得到首字符而非首个参数；package job 被依赖门阻止，无 artifact。[#35](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33350330934) 修正数组化后又暴露同一文件在 Windows 可分别表示为 `RUNNER~1` 短路径和 `runneradmin` 长路径，测试不应逐字比较路径别名；该 run 同样在源码门停止且无 artifact。
- [Windows release validation #36](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33350516767) 已让两套 Windows 源码 job 通过，package job 随后暴露 CI 夹具的 `Get-Command node.exe` 在同时存在 Node 20.18.3、24.19.0 和系统 Node 时返回多项，旧脚本把整个数组拼成一个命令名。失败发生在 CI 夹具准备，一键安装尚未运行，也没有上传 artifact；候选改为显式选择 `setup-node` 放在 PATH 首位的项并立即强制核对 `v20.18.3`。
- [Windows release validation #37](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33350818118) 在提交 `7f6c4c8` 上全绿，总耗时 5 分 3 秒：两套源码 job 各运行 83 项，82 项通过、1 项因托管 Windows 缺少 symlink 权限跳过；package job 用目标形态 Node v20.18.3 + 真实 npm Claude Code 2.1.84，另以原生 Claude 2.1.84 完成隔离门禁，然后一次运行顶层 launcher、阻断 Chrome/Claude Node 的 Internet 出站、保留 loopback、从人工扩展等待自动续跑并完成 MCP/Cookie E2E。下载 artifact 后，GitHub ZIP、迁移 tar、runtime tar 和两级 sidecar 均核对一致，四层 bundle 验证均为 `VALID`；这是 1.0.10 的首个可交付 Windows 制品。

- 私有仓库的 [Windows release validation #4](https://github.com/baitianxia/intranet-browser-agent/actions/runs/33299630904) 在提交 `98bdf62ee5c7b6e1f7a4866c0e146958e0f45680` 上成功，总耗时 3 分 25 秒。`windows-2022`、`windows-latest` 两套 PowerShell 5.1 源码 job 与 Windows 原生打包/门禁/安装 job 全部通过。
- 原生 job 安装并核对 Claude Code 2.1.84，使用批准的 Windows Node.js v24.19.0 最小 distribution 和 pnpm 11.19.0 构建。顶层 `INSTALL-WINDOWS-PILOT.cmd` 只运行一次，先通过发布门禁，再在同一进程链完成当前用户安装；后置步骤逐项确认门禁 PASS、安装 SUCCESS、安装摘要、`mcpScope=user`、空 `workspaceRoots`、无 `network` 白名单、真实 Claude user 配置和安装后运行时完整性。
- 该 run 生成 `intranet-browser-agent-transfer-1.0.8-windows-x64-33299630904`（36.8 MB，GitHub artifact SHA-256 `ebfd88b994af37207f1deff0bb16fd599ab73c919daedb9cafb002076bbb2afb`）和 `windows-validation-evidence-33299630904`（GitHub artifact SHA-256 `ac78e65985268b23c4dd3cedd0d0cb4990e0e110c668fe08ac2182edb41f9579`）。证据包含门禁日志、安装日志、安装摘要、实际清单和迁移包 `.sha256`。
- 在最终成功前，Actions 没有掩盖失败：run #1 暴露 NTFS ADS 测试夹具不可移植，run #2 暴露安装 Claude 前错误禁用更新，run #3 暴露 `cp1252` 无法输出中文状态文本。三项均在自动门禁/构建阶段定位并修复；其中注册事务失败时恢复临时 Claude 配置，安装没有被错误放行。
- 1.0.6 的早期人工 Windows 门禁曾暴露 CRLF 夹具缺陷；修复后的逐字节 LF/CRLF 用例现在同时在两套 Windows runner 通过。

## 当前验证环境

- 源码测试主机：macOS arm64，Python 3.14.6。
- 运行包构建工具：Node.js v24.19.0、pnpm 11.19.0、Python 3.12.13。
- PowerShell 解析器：官方 PowerShell 7.6.5 for macOS arm64。
- GitHub CI：`windows-2022` 与 `windows-latest` x64、Windows PowerShell 5.1 Desktop、Python 3.12；原生包 job 另使用 Node.js v24.19.0、pnpm 11.19.0 和 Claude Code 2.1.84。
- 包内 Node.js 为批准的官方 Windows x64 v24.19.0 最小 distribution；内网目标仍为 Windows x64 当前用户安装。

## 尚未验证，不能声称完成

- 企业内网终端上的 EDR/杀毒、组策略、应用白名单、代理/PAC、企业证书和现有 Claude 用户配置并发行为；GitHub 托管 runner 无法替代这些组织特定条件。
- 已有旧版本的真实内网终端升级、外部并发写入和故障注入回滚；这些路径已有自动事务测试，但尚未在企业端点进行破坏性演练。
- 企业目标机上的 Chrome/Edge、企业证书、PAC/代理、SSO/MFA 与真实业务页面读写行为；GitHub 托管 Chrome 的本地离线页面和 Cookie 登录态复用已通过，但不能替代这些组织特定条件。
- 企业 SCA、恶意代码扫描、制品签名、制品库导入及生产审批证据。

因此，1.0.13 及后续正式包的内网测试只运行顶层 `INSTALL-WINDOWS-PILOT.cmd` 一次；目标机不再替发布方运行通用 Windows 单元测试、AST 扫描或假 Claude 故障矩阵。单次安装事务仍会验证与目标实际状态有关的制品、现有 Claude、浏览器、Node、MCP 握手和真实注册，并在失败时回滚。生产放行仍需完成 `docs/acceptance.md` 的企业扫描、签名、浏览器/SSO 和生产治理项。
